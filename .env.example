# API process (default: 3100)
PORT=3100

# Optional unless you use /api/admin/* protected operations
ADMIN_API_KEY=change-me

# Optional: database location
DB_URL=file:unimarket.sqlite

# Optional: serve packages/web/dist from the API process
SERVE_WEB_DIST=false

# Optional: scheduler intervals (milliseconds)
RECONCILE_INTERVAL_MS=1000
SETTLE_INTERVAL_MS=60000
FUNDING_INTERVAL_MS=3600000
LIQUIDATION_INTERVAL_MS=5000
EQUITY_SNAPSHOT_INTERVAL_MS=300000

# Optional: browse result cache TTL (milliseconds, default 300000 = 5 minutes)
BROWSE_CACHE_TTL_MS=300000

# Optional: symbol display-metadata cache TTLs
SYMBOL_METADATA_TTL_MS=86400000
SYMBOL_METADATA_MISS_TTL_MS=600000

# Optional: perp risk settings
MAINTENANCE_MARGIN_RATIO=0.05

# Optional: trading fees (decimal rate, [0, 1))
# If omitted, default taker fee is 0.
DEFAULT_TAKER_FEE_RATE=0
# Market-specific overrides (take precedence over DEFAULT_TAKER_FEE_RATE)
HYPERLIQUID_TAKER_FEE_RATE=0
POLYMARKET_TAKER_FEE_RATE=0
