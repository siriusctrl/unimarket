{
  "name": "unimarket",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "corepack pnpm -r build",
    "check": "corepack pnpm verify:docs && corepack pnpm typecheck && corepack pnpm test",
    "dev": "corepack pnpm --parallel --filter @unimarket/api --filter @unimarket/web dev",
    "dev:api": "corepack pnpm --filter @unimarket/api dev",
    "dev:web": "corepack pnpm --filter @unimarket/web dev",
    "dev:renderer": "corepack pnpm --filter @unimarket/renderer dev",
    "setup:browsers": "playwright install chromium",
    "verify:ui": "playwright test",
    "verify:preview": "node scripts/verify-preview.mjs",
    "verify:proof": "node scripts/record-proof.mjs",
    "verify:analysis-live": "node scripts/verify-live-analysis.mjs",
    "verify:docs": "node scripts/check-docs.mjs",
    "verify": "corepack pnpm check && corepack pnpm build && corepack pnpm verify:ui && corepack pnpm verify:preview",
    "render:analysis": "node scripts/render-analysis.mjs",
    "smoke:api": "bash scripts/smoke-api.sh",
    "coverage": "corepack pnpm --filter @unimarket/analysis test:coverage && corepack pnpm --filter @unimarket/core test:coverage && corepack pnpm --filter @unimarket/markets test:coverage && corepack pnpm --filter @unimarket/api test:coverage && corepack pnpm --filter @unimarket/renderer test:coverage && corepack pnpm --filter @unimarket/web test:coverage",
    "test": "corepack pnpm -r test",
    "typecheck": "corepack pnpm -r typecheck"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.13.9",
    "@vitest/coverage-v8": "^3.2.4",
    "tsx": "^4.20.5",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
